const React = require("react");
const ReactDom = require("react-dom/client");

const ReactionSpeed = require("./reactionSpeed");

ReactDom.createRoot(document.querySelector("#root")).render(<ReactionSpeed/>);